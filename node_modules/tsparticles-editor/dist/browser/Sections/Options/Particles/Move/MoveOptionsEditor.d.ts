import { EditorGroup } from "object-gui";
import type { Container } from "tsparticles";
import { EditorBase } from "../../../../EditorBase";
export declare class MoveOptionsEditor extends EditorBase {
    group: EditorGroup;
    private options;
    constructor(particles: Container);
    addToGroup(parent: EditorGroup): void;
    private addAngle;
    private addAttract;
    private addDistance;
    private addGravity;
    private addOutModes;
    private addPath;
    private addTrail;
    private addProperties;
}
