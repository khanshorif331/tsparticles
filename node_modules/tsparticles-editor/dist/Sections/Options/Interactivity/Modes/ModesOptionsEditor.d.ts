import type { Container } from "tsparticles";
import { EditorGroup } from "object-gui";
import { EditorBase } from "../../../../EditorBase";
export declare class ModesOptionsEditor extends EditorBase {
    group: EditorGroup;
    private options;
    constructor(particles: Container);
    addToGroup(parent: EditorGroup): void;
    private addAttract;
    private addBubble;
    private addConnect;
    private addGrab;
    private addLight;
    private addPush;
    private addRemove;
    private addRepulse;
    private addSlow;
    private addTrail;
}
