import type { Container } from "tsparticles";
import { Editor } from "object-gui";
export declare class ParticlesEditor extends Editor {
    readonly particles: Container;
    constructor(particles: Container);
    private _presets?;
    protected customize(): void;
    addPreset(text: string, file: string): void;
    private addOptions;
    private addButtons;
    private addPresets;
}
